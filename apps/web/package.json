{
  "name": "@nebula/web",
  "version": "0.1.0",
  "private": true,
  "description": "Nebula dashboard: vault stats, oracle attestations and reputation, x402 sales, and the full on-chain run replay with explorer links.",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.7",
    "typescript": "^5.8.2"
  }
}
